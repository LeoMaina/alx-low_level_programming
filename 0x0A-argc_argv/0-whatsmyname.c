#include "main.h"
#include <stdio.h>

/**
 * main - entry point
 * @argc: count of arguments
 * @argv: pointer to string
 *
 * Return: 0
 */
int main(int __attribute__ ((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
