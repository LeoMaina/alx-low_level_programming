#include "main.h"

/**
 * _abs - outputs the absolute value of a number
 *
 * @int: - integer under check
 *
 * Return: 0
 */

int _abs(int n)
{

	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
